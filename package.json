{
  "name": "webshoptesting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "login": "npx cypress run --e2e  --env grepTags=@login",
    "smoke": "npx cypress run --e2e  --env grepTags=@smoke",
    "order": "npx cypress run --e2e  --env grep=\"order\"",
    "wishlist": "npx cypress run --e2e  --env grep=\"wishlist\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cypress/grep": "^4.0.1",
    "cypress": "^13.5.1"
  }
}